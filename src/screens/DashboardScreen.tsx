import React from 'react';
import { View } from 'react-native';

const DashboardScreen = (): JSX.Element => (
  <View style={{
    flex: 1, justifyContent: 'center',
  }}
  >
    <div>Ceci est le dashboard</div>
  </View>
);
export default DashboardScreen;